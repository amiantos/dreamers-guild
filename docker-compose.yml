version: '3.8'

services:
  aislingeach-web:
    build: .
    ports:
      - "8005:8005"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8005
    restart: unless-stopped
